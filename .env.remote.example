# =============================================================================
# Remote Deploy Configuration
# Copy to .env.remote and customize for your Docker host.
# Used by remote-deploy.sh to SSH into the host and run deploy.sh.
# =============================================================================

# SSH host alias (configured in ~/.ssh/config) — REQUIRED
REMOTE_HOST=your-docker-host

# Path to hangar checkout on the remote host
REMOTE_PATH=~/hangar
